services:
  db:
    image: postgres:16-alpine
    ports:
      - '5433:5432'
    environment:
      POSTGRES_DB: agendly
      POSTGRES_USER: agendly
      POSTGRES_PASSWORD: agendly_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
